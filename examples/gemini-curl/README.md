Using the questions generate in ../question - create a script that
uses curl to call the Gemini API to answer those questions. (You will
need to have a GEMINI_API_KEY environment variable set up before you
can run the script).
